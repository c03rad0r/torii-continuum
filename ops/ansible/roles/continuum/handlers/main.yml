---
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart continuum-agent
  ansible.builtin.systemd:
    name: continuum-agent
    state: restarted

- name: reload nginx
  ansible.builtin.systemd:
    name: nginx
    state: reloaded
