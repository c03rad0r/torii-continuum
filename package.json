{
  "name": "torii-continuum",
  "version": "0.2.16-alpha",
  "description": "Continuum — an app builder, project engine and marketplace for bot work. A gateway into the Torii ecosystem.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "cd tests/playwright && npx playwright test --config=playwright.config.ts",
    "test:vitest": "vitest run"
  },
  "devDependencies": {
    "vite": "^5.4.21",
    "vitest": "^2.1.9"
  },
  "dependencies": {
    "nostr-tools": "^2.23.9"
  }
}
